a <= not '0';
B <= not '1';
c <= d and '0';
Z <= '0' and f;
g <= h and '1';
i <= '1' and j;
k <= l or '0';
m <= '0' or n;
o <= p or '1';
q <= '1' or r;
s <= t and t;
u <= v or v;
w <= x or  not x;
y <= z or (z and a);
b <= (c and d) or c;
e <= f and (f or g);
h <= (i or j) and j;
